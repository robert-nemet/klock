apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "klock.fullname" . }}-test-connection"
  labels:
    {{- include "klock.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "klock.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
